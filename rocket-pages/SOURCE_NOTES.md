# Source notes

The Python files in `source/` are the code cells from the uploaded notebooks, preserved with comments.

Browser-specific adapter files are explicitly named `*_browser.py` and `browser_adapter.py` so the boundary between original simulation code and UI plumbing is visible.
